apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: multi-tier-app
data:
  DB_HOST: "database-service"
  DB_PORT: "3306"
  DB_NAME: "webapp_db"
  DB_USER: "webapp_user"
  APP_PORT: "5000"
  APP_ENV: "production"
